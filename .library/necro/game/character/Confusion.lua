--- @meta

local Confusion = {}

function Confusion.isConfused(entity) end

function Confusion.inflict(entity, turns) end

function Confusion.clear(entity) end

function Confusion.apply(entity, direction) end

return Confusion
