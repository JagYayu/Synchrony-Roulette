--- @meta

local Facing = {}

function Facing.setVisualDirection(entity, direction) end

function Facing.setGameplayDirection(entity, direction) end

function Facing.setDirection(entity, direction) end

return Facing
