--- @meta

local LowPercent = {}

function LowPercent.isActive() end

function LowPercent.isActiveInCurrentLoop() end

function LowPercent.setActive(active) end

function LowPercent.isEnforced() end

function LowPercent.negate(entity, item) end

return LowPercent
