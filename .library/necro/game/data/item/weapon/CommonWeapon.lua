--- @meta

local CommonWeapon = {}

--- Deprecated, this is now done automatically
function CommonWeapon.pattern(pattern) end

function CommonWeapon.postProcessPattern(entity, componentName) end

return CommonWeapon
