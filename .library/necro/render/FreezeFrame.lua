--- @meta

local FreezeFrame = {}

function FreezeFrame.reset() end

function FreezeFrame.playHitstop(entity, duration, delay) end

function FreezeFrame.playHitstopAt(x, y, duration, delay) end

function FreezeFrame.isActive() end

function FreezeFrame.getVisualMusicTime() end

return FreezeFrame
