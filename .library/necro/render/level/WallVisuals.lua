--- @meta

local WallVisuals = {}

--- @return string
function WallVisuals.getTexture(tileID, x, y) end

function WallVisuals.getColor(brightness) end

function WallVisuals.getWallSprite(tileID, x, y) end

return WallVisuals
